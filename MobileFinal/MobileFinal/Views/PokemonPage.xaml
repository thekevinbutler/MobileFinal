<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:behaviors="clr-namespace:Prism.Behaviors;assembly=Prism.Forms" xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" prism:ViewModelLocator.AutowireViewModel="True" x:Class="MobileFinal.Views.PokemonPage">
    <StackLayout HorizontalOptions="Center" 
        Padding="0,15,0,0"> 
    <Button Text="Back"
         Command="{Binding BackToMainCommand}"/>
    <Label Text="{Binding PokeDisplay.name}"/>
        <Image Source ="{Binding PokeDisplay.sprites.front_default}"
         Scale="10" Aspect="Fill" 
        HorizontalOptions="EndAndExpand"
        VerticalOptions="EndAndExpand" />
    </StackLayout>
</ContentPage>
